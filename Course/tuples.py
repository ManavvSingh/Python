# Tuples are similar to lists but they are immutable (Can't modify)

subjects = ("math","sci","eng")
print(type(subjects))

# subjects[0] = "social"  Will throw an error

print(len(subjects)) # Prints the length of the tuple
del subjects # Deletes the tuple completely
print(subjects)


